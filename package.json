{
  "name": "slides",
  "type": "module",
  "scripts": {
    "build": "bun build public/js/app.js --outdir public/dist --bundle --format esm --external html2canvas --external qrcodejs --external '/js/slides/*'",
    "dev": "bun --watch server/index.ts",
    "start": "bun server/index.ts"
  },
  "dependencies": {
    "marked": "^9.1.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260531.1",
    "@happy-dom/global-registrator": "^20.9.0",
    "@types/bun": "latest"
  }
}
