# Test preloads are scoped per-file via direct `import` statements (see
# public/js/paginator.test.ts). Global preload would inject happy-dom into
# storage / drust tests too and block their cross-origin fetches.
